annotations

@Overrid

@SupressWarning()  - to rell the compiler to ignore specifi warnings for a particular part of your code
                   used on methods, classes, fields, parameter, constructor, or variables too

@SafeVarargs - to avoid compiler warnings about unsafe operations on variable length argumenets
              unchecked or unsafe operations
              cannot be used on overriding methods lead to unsafe operarions
              applies only to static final or private methods and constructor 
               No compiler "unsafe operation" warning because of @SafeVarargs


