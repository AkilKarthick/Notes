JDBC

step1:need to load the database driver in jdbc
  class.forName("org.hsqldb.hjdbcDriver"); //regsitering the driver

Step2: connect to the database or establise a connectio with database
  
try(Connection conn = DriverManager.getConnection("JDBC:]hsqlDdb:hsql://locahost/","sa",""))

step3: createStatement()

step4: send sql statement to the database
stmt.execute(sql)
