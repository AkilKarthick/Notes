select emp_id, salary, Rank() over(order by salary DESC) as salary_rank
from employee;

window function - used to perform calucalution across a set of table row that
somehow relategd to current row

row_number
rank()
dense_rank()

lead()
lag()
sum() average() count() over() 
