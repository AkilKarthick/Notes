sortingAnArray
bubble sort
how this work>
keeps asking the next one are you bigger then me? if yes lets switch place 
this contineu untill the  biggest elements slowly bubbles to end
--------------

int[] arr ={1,2,3,8,12,45};
Arrays.sort(arr); //ascending order
System.out.println("sorted arrays:" + arr.toString());

//descending order
Arrays.sort(arr,Collections.reverseOrder());

for List:
List<Integer> s1 = Arrays.asList(1,2,3,4,5,12);
Collections.sort(s1,Collections.reverseOrder()); //desc
