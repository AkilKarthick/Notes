day01

1. find the second highest number in an array?

int[] arr = {1,2,3,4,5,6,7};
Optional<Integer> secondHighestnum = Arrays.stream(arr)
  .boxed().sorted(Comparator.reverseOrder())
.distinct().skip(1).findFirst();

//using normal loops?

2. find duplicate elements in a list?

int[] arr1 = [1,2,3,4,5,1,2];
Set<Integer> dupelements = new HashSet<>();
Arrays.stream(arr1).boxed()
.filter(n->)dupelements.add(n).collect(Collectors.toList()));


3. REVERSE /sort/ac or des an Array whthout any inbuilt method?

int[] arr = {1,2,3,4,5,6};
int[] rev = arr[i];
for(int i = arr.length -1; i>=0; i--){
  rev+ = arr[i];
}  wrong

use a loop no extra class? how 
int[] arr = {1,2,3,4,5,6};

int start =0;
int end = arr.length-1;
while(start < end) {
  int temp = arr[start];
  arr[stop] = arr[end];
  arr[end] = temp;
start++;
end--;
}

4. find max/min and minmum element in an array?

int[] arr = {2,4,6,8,9};
int max = arr[o];
int min = arr[o];

for(int i=0; i<arr.length;i++){
  if(arr[i] > arr[max]){ //wrong
  correct one is
  if(arr[i] > max)
   max=arr[i];
  }
if(arr[i] < arr[min])
  min=arr[i];
}
used foreach loop to print

//or 
using Math  class to find max/min
for(int num:arr){
max = Math.max(max,num);
min = Math.min(min,num);
}

RotateArray using java 8

int k=2 // rotate right side by 2
List<Integer> list = Arrays.stream(arra).boxed().collect(Collectors.toList);
Collections.rotate(list,k);

rotate left 

List<Integer> rotatedList = Stream.concate(list.subList(k,list.size()).stream, list.subList(0,k).stream()).collect(Collectors.toList());

//frequenc of each element in an array
		
		Map<Integer,Long> freqmap =Arrays.stream(arr123)
										  .boxed()
										  .collect(Collectors.groupingBy(n->n, 
												  Collectors.counting()));
		
		freqmap.forEach((num,count)->System.out.println("num :"+num +"count: " +count));

//to find second smallest number using 
		
		int secondSmallest =Arrays.stream(arr123)
								   .boxed()
								   .sorted(Comparator.naturalOrder())
								   .distinct()
								   .skip(1)
								   .findFirst().get();
		System.out.println(secondSmallest);

